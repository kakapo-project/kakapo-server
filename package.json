{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@handsontable/react": "^2.0.0",
    "codemirror": "^5.40.2",
    "handsontable": "^6.1.1",
    "lodash": "^4.17.11",
    "react": "^16.5.2",
    "react-codemirror": "^1.0.0",
    "react-dom": "^16.5.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.0.5",
    "semantic-ui-css": "^2.4.1",
    "semantic-ui-react": "^0.83.0"
  },
  "scripts": {
    "start-fe": "cd client && react-scripts start",
    "start-be": "cargo run",
    "build": "cargo build --release && rm -rf ./www/ && cd client && react-scripts build && mv build ../www/",
    "test": "cargo test && cd client && react-scripts test",
    "eject": "cd client && react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
