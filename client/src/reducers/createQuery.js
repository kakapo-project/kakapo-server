import { ACTIONS } from '../actions'

const initialState = {

}

const createQuery = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default createQuery