import { ACTIONS } from '../actions'

const initialState = {

}

const createScript = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default createScript