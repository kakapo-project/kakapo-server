import { ACTIONS } from '../actions'

const initialState = {

}

const createTable = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default createTable